# 利用激光雷达数据辅助风机功率预测

## 项目背景

峡沙56号风机机舱上架设了一台**激光雷达（LiDAR）**，可以测量风机前方不同距离处（如 100 m、150 m、200 m 等）的气象特征（水平风速 HWS、垂直风速 VWS、风向等）。与此同时，56 号风机自身通过 **SCADA** 系统记录风速、风向、有功功率等运行数据，南瑞（**NARI**）平台也对同一台风机的数据进行采集和导出。

本项目旨在利用激光雷达的前瞻性测量信息，探索其对风机功率预测的辅助作用，主要研究问题包括：

- 激光雷达数据是否能提升风机功率预测精度？
- 哪些距离下的激光雷达数据最有参考价值？
- 不同预测时长下，激光雷达数据的辅助效果有何差异？
- 哪些特征变量（如 HWS、VWS、TI 等）对功率预测贡献最大？

---

## 项目结构

```
利用激光雷达数据辅助风机功率预测/
├── README.md                                 # 本文件
├── 情况说明和待办.md                          # 任务说明与待办事项
├── align_timestamps.py                       # SCADA 与 NARI 数据时间戳对齐脚本
├── analysis_figures.png                      # SCADA vs NARI 时间戳分析图
├── 峡沙56号_SCADA与南瑞数据时间戳对齐分析报告.md  # 时间戳对齐详细分析报告
│
├── RAW_DATA/                                 # 原始数据（部分文件已截取若干行）
│   ├── #56风机功率_2024.csv                  # 56 号风机 SCADA 导出有功功率（1 分钟粒度）
│   ├── #56风机故障时刻.csv                   # 56 号风机故障时刻记录
│   ├── #56风机状态_2024.xlsx                 # 56 号风机状态数据
│   ├── 合并后的风机雷达数据.csv              # 风机 SCADA + 激光雷达合并原始数据
│   ├── 峡沙#56号风机20240830-20241121_南瑞.csv  # 南瑞（NARI）平台导出数据
│   └── MySE6.45-180推力曲线.csv             # 机型推力曲线
│
├── DATA/                                     # SCADA 与 NARI 对齐分析的中间数据
│   ├── 峡沙56号_合并风机导出数据和南瑞数据.csv  # SCADA + NARI 合并数据（对齐前）
│   └── 峡沙56号_时间戳对齐后数据.csv          # SCADA + NARI 对齐后数据（推荐使用）
│
├── PROCESS_DATA/                             # 各阶段处理后的数据
│   ├── #1-1合并风机导出数据和南瑞数据.csv      # Step 1-1 输出
│   ├── #2-1对齐风机导出数据和南瑞数据.csv      # Step 2-1 输出（时间戳对齐后）
│   ├── #3峡沙56号_时间戳对齐后数据_10分钟均值.csv  # Step 3 输出（10 分钟均值）
│   ├── #4峡沙56号_风机-激光雷达数据合并.csv    # Step 4 输出（风机 + 激光雷达合并）
│   ├── #6不是所有距离HWS大于60且非故障的时间戳.csv  # Step 6 输出（有效率统计）
│   ├── #6不是都大于60的时间戳.csv             # Step 6 输出（补充）
│   └── #7构建好的训练数据集.csv               # Step 7 输出（最终训练集）
│
└── CODE/                                     # 分析与处理脚本（按执行顺序编号）
    ├── #1-1合并风机导出数据和南瑞数据.py       # 合并 SCADA 与 NARI 数据
    ├── #1-2DASH对比风机数据和南瑞数据.py       # 可视化对比（对齐前）
    ├── #2-1对齐风机导出数据和南瑞数据.py       # 时间戳对齐处理
    ├── #2-2DASH对比风机数据和南瑞数据.py       # 可视化对比（对齐后）
    ├── #3时间粒度1变10.py                     # 将 1 分钟数据重采样为 10 分钟均值
    ├── #4风机-激光雷达数据合并.py             # 合并风机数据与激光雷达数据
    ├── #5风速功率散点图DASH.py               # 风速-功率散点图可视化
    ├── #6统计不同距离数据的有效率.py          # 统计各距离激光雷达数据有效率
    ├── #7提取所有距离数据功率同时有效.py      # 提取各距离数据与功率同时有效的时间段
    ├── #8功率预测模型对比.py                  # ★ 单步预测：LightGBM/XGBoost/LSTM 对比（当前快照→+10min）
    ├── #8功率预测实验报告.md                  # ★ 单步预测实验方案、结果分析与结论
    ├── #9多步功率预测.py                      # ★★ 多步预测：历史时序输入→未来+10/+20/+30/+60min
    └── #9多步功率预测实验报告.md              # ★★ 多步预测实验方案、结果分析与结论（含场景1/场景2对比）
```

---

## 数据说明

### 时间范围

| 数据集 | 时间范围 | 时间粒度 |
|--------|---------|---------|
| SCADA 有功功率 | 2024 年全年 | 1 分钟 |
| NARI 平台数据 | 2024-08-30 ~ 2024-11-20 | 1 分钟 |
| 激光雷达数据 | 与风机数据对应 | 10 分钟 |

> **注意**：RAW_DATA 和 PROCESS_DATA 中的部分文件仅为截取的片段，实际完整数据行数更多。

### SCADA 与 NARI 时间戳问题

经互相关分析发现，南瑞（NARI）平台导出数据相对于 SCADA 数据存在**系统性时间戳滞后**，且随时间段变化：

| 时段 | NARI 滞后量 |
|------|-----------|
| 2024-08-30 ~ 2024-09-30 | +2 分钟 |
| 2024-10-01 ~ 2024-11-07 17:59 | +3 分钟 |
| 2024-11-07 18:00 ~ 2024-11-20 | +4 分钟 |

分段变步长对齐后，Pearson 相关系数从 0.9767 提升至 **0.9957**，RMSE 从 449.22 kW 降低至 **193.54 kW**。
详见 [峡沙56号_SCADA与南瑞数据时间戳对齐分析报告.md](./峡沙56号_SCADA与南瑞数据时间戳对齐分析报告.md)。

---

## 数据处理流程

```
RAW_DATA/
  #56风机功率_2024.csv          ──┐
  峡沙#56号风机..._南瑞.csv    ──┤  Step 1-1: 合并 SCADA + NARI
                                  ↓
                        PROCESS_DATA/#1-1合并...csv
                                  │
                           Step 1-2: DASH 可视化对比（对齐前）
                                  │
                           Step 2-1: 时间戳对齐（分段变步长）
                                  ↓
                        PROCESS_DATA/#2-1对齐...csv
                                  │
                           Step 2-2: DASH 可视化对比（对齐后）
                                  │
                           Step 3: 1 分钟 → 10 分钟均值
                                  ↓
                        PROCESS_DATA/#3...10分钟均值.csv
                                  │
RAW_DATA/合并后的风机雷达数据.csv ─┤  Step 4: 合并风机 + 激光雷达
                                  ↓
                        PROCESS_DATA/#4...风机-激光雷达数据合并.csv
                                  │
                     ┌────────────┴────────────┐
                Step 5              Step 6                Step 7
          风速-功率散点图     统计各距离数据有效率    提取全距离同时有效数据
                                                          ↓
                                              PROCESS_DATA/#7构建好的训练数据集.csv
```

---

## 运行说明

### 环境依赖

```bash
pip install pandas numpy scikit-learn lightgbm xgboost matplotlib tensorflow
```

### 执行顺序

按脚本编号顺序依次执行 `CODE/` 目录下的脚本：

```bash
cd CODE/

python "#1-1合并风机导出数据和南瑞数据.py"
python "#1-2DASH对比风机数据和南瑞数据.py"    # 启动 Dash 服务，浏览器查看
python "#2-1对齐风机导出数据和南瑞数据.py"
python "#2-2DASH对比风机数据和南瑞数据.py"    # 启动 Dash 服务，浏览器查看
python "#3时间粒度1变10.py"
python "#4风机-激光雷达数据合并.py"
python "#5风速功率散点图DASH.py"              # 启动 Dash 服务，浏览器查看
python "#6统计不同距离数据的有效率.py"
python "#7提取所有距离数据功率同时有效.py"
python "#8功率预测模型对比.py"                # ★ 单步预测：LightGBM/XGBoost/LSTM 对比（快照输入→+10min）
python "#9多步功率预测.py"                    # ★★ 三层分层实验（v3：34场景，含层1无SCADA对比）
```

### 单独运行时间戳对齐脚本

```bash
# 使用默认路径（推荐）
python align_timestamps.py

# 自定义输入输出路径
python align_timestamps.py \
    --input  DATA/峡沙56号_合并风机导出数据和南瑞数据.csv \
    --output DATA/峡沙56号_时间戳对齐后数据.csv
```

---

## 主要发现

### 激光雷达各距离数据有效率

激光雷达在 40 m ~ 300 m 的 10 个距离层进行测量。经有效率筛选后，约 **9,927 个 10 分钟时间戳**（覆盖 2024-08-30 ~ 2024-11-20）的各距离数据均同时有效，作为建模数据集。数据中存在 **140 处时间戳间隙**，已切分为 143 个连续时间段，LSTM 滑动窗口仅在连续段内构建。

### 功率预测方案说明

详细实验方案与结果分析见 [`CODE/#8功率预测实验报告.md`](CODE/#8功率预测实验报告.md)。

**预测设置**：
- **目标变量**：`ACTIVE_POWER_#56_对齐_前10分钟均值`（南瑞平台数据，kW）
- **预测步长**：+10 min（单步超前）
- **输入特征**：机舱风速（SCADA）、各距离激光雷达风速（HWS）、当前时刻功率（自回归）
- **不使用风向特征**：当前风向数据质量较差，暂不纳入

### 功率预测模型性能评估（预测步长 +10 min）

基于 **9,864 个有效时间戳**（训练集 80%、测试集 20%，按时间顺序划分）：

**最终性能排名：**

| 模型 | 特征组合 | RMSE (kW) | MAE (kW) | R² |
|------|---------|-----------|----------|-----|
| **LightGBM** | **SCADA HWS + 激光雷达 90 m HWS + 当前功率** | **433.3** | **275.3** | **0.9630** |
| XGBoost | SCADA HWS + 激光雷达 60 m HWS + 当前功率 | 433.4 | 271.4 | 0.9630 |
| XGBoost | SCADA HWS + 当前功率（Baseline） | 438.1 | 281.1 | 0.9622 |
| LightGBM | SCADA HWS + 当前功率（Baseline） | 453.1 | 287.8 | 0.9595 |
| LSTM | SCADA HWS + 当前功率 | 582.7 | 384.7 | 0.9318 |

### 最优辅助距离分析

逐距离测试激光雷达 HWS 对预测精度的影响（LightGBM，SCADA HWS + 该距离 HWS + 当前功率）：

| 距离 (m) | RMSE (kW) | R² | vs 基准降幅 |
|---------|-----------|-----|-----------|
| **40**  | 440.5 | 0.9618 | −2.8% |
| **60**  | 434.7 | 0.9628 | −4.1% |
| **90**  | **433.3** | **0.9630** | **−4.4%** ✅ |
| 120 | 438.6 | 0.9621 | −3.2% |
| 150 | 437.8 | 0.9622 | −3.4% |
| 180 | 435.2 | 0.9627 | −4.0% |
| 210 | 436.6 | 0.9624 | −3.6% |
| 240 | 444.1 | 0.9611 | −2.0% |
| 270 | 446.9 | 0.9606 | −1.4% |
| 300 | 454.2 | 0.9593 | +0.2% |

> **最优单距离：90 m**（LightGBM）/ **60 m**（XGBoost），近距离激光雷达风速与 10 min 后功率的相关性最强；远距离（≥240 m）的增益快速减弱，300 m 甚至略低于 SCADA 基准。

### 关键结论

1. **激光雷达风速有效但增益有限**：最优距离（90 m）使 LightGBM RMSE 从 453.1 kW 降至 433.3 kW（**↓4.4%**）。当前功率（自回归特征）已包含大量信息，激光雷达的边际贡献较小。

2. **近距优于远距**：60~90 m 的激光雷达风速与近期功率相关性最强，符合风机空气动力学响应时间规律；240 m 以上的远距离风速几乎无增益。

3. **多距离组合不如单距离**：堆叠多个距离的 HWS 引入冗余特征，LightGBM 性能反而下降；推荐使用单一最优距离（90 m）。

4. **气象特征暂无增益**：VShear、HShear、TI 等气象特征在当前数据量下未能提升性能，反而导致轻微过拟合，暂不建议使用。

5. **树模型优于 LSTM**：在约 1 万条 10 分钟粒度数据上，LightGBM/XGBoost 显著优于 LSTM；LSTM 更适合数据量更大的场景。

### 图表输出

运行 `CODE/#8功率预测模型对比.py` 后，`PROCESS_DATA/` 目录下将生成：

| 文件 | 内容 |
|------|------|
| `#8_exp1_distance_vs_rmse.png` | Exp-1：逐距离 LiDAR HWS 对 RMSE 的影响（与 SCADA 基准对比） |
| `#8_exp_comparison.png` | LightGBM 所有实验 RMSE / R² 综合排名 |
| `#8_model_comparison.png` | 关键实验 LightGBM/XGBoost/LSTM 性能对比 |
| `#8_feature_importance.png` | LightGBM 全特征模型 Top-30 特征重要性 |
| `#8_pred_vs_actual.png` | LightGBM Baseline 预测值 vs 实际值散点图 |
| `#8模型对比结果.csv` | 全部实验的 RMSE / MAE / R² / N 数据表 |

---

## 多步预测实验（#9 第三版：层1补充无SCADA对比组）：历史时序输入 → 未来多步输出

详细实验方案与全量结果见 [`CODE/#9多步功率预测实验报告.md`](CODE/#9多步功率预测实验报告.md)。

**实验设置**：
- **输入**：历史 60 分钟时序（6 步 × 10 min）
- **输出**：未来 +10 / +20 / +30 / +60 分钟四个步长的功率
- **时间连续性约束**：滑动窗口仅在连续时间段内构建（不跨越 140+ 处时间戳间隙）

**三层实验设计**（34 个场景，树模型全跑，LSTM 跑 4 个关键场景）：

| 层 | 场景 | 说明 |
|----|------|------|
| 层1 M组 | M0：仅SCADA | 无激光雷达基准 |
| 层1 M组 | M1（10个）：单距离（40~300m）+ SCADA | 逐距离扫描 |
| 层1 M组 | M2/M3/M4/M5：近/中/远/全距离 + SCADA | 距离分组 |
| 层1 **N组**（新增）| N1（10个）：单距离（40~300m），**无SCADA** | 逐距离扫描，回答干扰问题 |
| 层1 **N组**（新增）| N2/N3/N4：近/中/远距离，**无SCADA** | 距离分组无SCADA |
| 层2 | E1~E4：M5 + VShear / HShear / TI / 全气象 | 气象特征增益实验 |
| 层3 | L3a：仅全LiDAR HWS；L3b：仅LiDAR HWS+气象 | 去 SCADA 工程替代验证 |

### 多步预测关键结果（RMSE, kW）

**全局最优：LSTM + M0（仅 SCADA 机舱风速 + 历史功率）**

| 模型 | 场景 | +10 min | +20 min | +30 min | +60 min |
|-----|------|---------|---------|---------|---------|
| **LSTM** | **M0: 仅SCADA** | **427.4** | **584.9** | **693.6** | **881.8** |
| LightGBM | **N1_270m_only（仅LiDAR，无SCADA）** | **438.4** | — | — | — |
| LightGBM | **N3_mid_only（仅中距，无SCADA）** | **438.8** | **633.4** | **755.8** | **969.9** |
| LightGBM | M0: 仅SCADA | 443.8 | 622.9 | 744.5 | 941.1 |
| XGBoost | M0: 仅SCADA | 431.0 | 607.5 | 729.2 | 930.6 |

### 七项关键结论（第三版更新）

1. **LiDAR+SCADA 效果差的主因是"信号干扰"**：N1（仅LiDAR，无SCADA）的 150~300m 距离在 +10min 上全部超越了 M0（仅SCADA），但 M1（LiDAR+SCADA）却没能超越，证明是 LiDAR 与 SCADA 相互干扰导致性能下降，而非 LiDAR 本身信息不足。

2. **中远距 LiDAR（150~300m）与 SCADA 机舱风速存在冗余干扰**：Δ RMSE 热力图显示，150~300m 距离段去掉 SCADA 后 RMSE 普遍下降（绿色区域），说明这段距离的 LiDAR 与 SCADA 高度相关，混用引入冗余噪声。

3. **仅中距 LiDAR（N3_mid，150-210m，无SCADA）在全步长超越所有含SCADA方案**（+10min=438.8，优于M0的443.8）。

4. **多步 LSTM 全面超越树模型**（与 #8 单步结论相反）：随步长增大差距扩大。

5. **气象特征（VShear/HShear/TI）在多步设置中无明显增益**，TI 在 LightGBM 中明显有害（+60min RMSE +34kW）。

6. **LSTM 下去SCADA（L3a）在+10/+20min 优于 LiDAR+SCADA（M5）**，但仍不如最优的 M0。

7. **近距 LiDAR（40~120m）的 SCADA 干扰较小**：Δ RMSE 在近距为正（SCADA 有益），说明近距 LiDAR 与 SCADA 具有一定独立性，两者互补而非干扰。

### 多步预测图表输出（第三版，13 张）

| 文件 | 内容 |
|------|------|
| **`#9_L1a_距离扫描_LightGBM.png`** | **双子图：含SCADA(左) vs 无SCADA(右) 逐距离扫描对比** |
| **`#9_L1b_距离组合热力图_LightGBM.png`** | **M组+N组并排热力图（白线分隔，含L3a=N5）** |
| **`#9_L1c_SCADA干扰量_LightGBM.png`** | **新增：SCADA干扰量 Δ RMSE 热力图（红=有益，绿=干扰）** |
| `#9_L1a/L1b/L1c_*_XGBoost.png` | 同上，XGBoost |
| `#9_L2_气象特征增益_LightGBM/XGBoost.png` | 层2气象特征增益柱状图 |
| `#9_L3_去SCADA对比.png` | 层3 M0/M5/L3a/L3b 折线对比（LightGBM+LSTM） |
| `#9_LSTM_关键场景.png` | LSTM 关键场景 RMSE/R² |
| `#9_全场景热力图_层1/2/3.png` | LightGBM 全场景热力图（层1现为28行）|
| `#9多步预测结果.csv` | 288 条实验记录（34 场景 × 4 步长 × 多模型）|
